# 🐟 UserFish
## 📌 Описание 
Этот проект представляет собой простой CRUD-сервис на Java с использованием Hibernate ORM и PostgreSQL.
### Возможности
✅ Создание новых пользователей

✅ Просмотр информации о пользователя

✅ Обновление данных пользователей

✅ Удаление пользователей

✅ Просмотр списка всех пользователей

### Технологии

- **Java 24+**
- **Hibernate 6.3.1** - ORM фреймворк
- **PostgreSQL** - реляционная база данных
- **Maven** - управление зависимостями
- **Logback** - логирование приложения
- **DAO паттерн** - архитектура доступа к данным

## ⚙️ Настройка подключения к своей базе данных
### 1. Создайте базу данных PostgreSQL
```sql
CREATE DATABASE userdb;
```
Вы можете использовать другое имя — просто укажите его в конфигурации.

### 2. Настройте файл hibernate.cfg.xml

Найдите блок параметров:
```xml
<property name="connection.driver_class">org.postgresql.Driver</property>
<property name="connection.url">jdbc:postgresql://localhost:5432/userdb</property>
<property name="connection.username">postgres</property>
<property name="connection.password">your_password</property>
```
Измените параметры под свою среду:


| Параметр | Описание | Пример значения |
|----------|-----------|-----------------|
| `connection.url` | URL базы данных | `jdbc:postgresql://localhost:5432/userdb` |
| `connection.username` | Имя пользователя | `postgres` |
| `connection.password` | Пароль пользователя | `password` |

## ▶️ Запуск проекта

### 1. Сборка проекта
Cкомпилируйте все классы и проверьте зависимости следующей командой
```bash
mvn clean install compile
```

### 2. Запуск Main-класса
Запустите главный класс следующей командой:
```bash
mvn exec:java
```

## 🗂️ Структура проекта

```
user-service/
├── src/main/java/com/userfish/
│   ├── dao/           # Data Access Object слои
│   ├── model/         # Сущности (User)
│   ├── util/          # Утилиты (HibernateUtil)
│   └── Main.java      # Главный класс приложения
├── src/main/resources/
│   ├── hibernate.cfg.xml  # Конфигурация Hibernate
│   └── logback.xml        # Конфигурация логирования
└── pom.xml            # Конфигурация Maven
```
